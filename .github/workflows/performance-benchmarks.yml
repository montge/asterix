name: Performance Benchmarks

# TEMPORARILY DISABLED
# This workflow has YAML syntax issues due to inline Python code blocks.
# The multiline Python strings break YAML parsing (block scalar indentation).
# TODO: Refactor inline Python code to external scripts in scripts/ directory

on:
  workflow_dispatch:
    inputs:
      compare_baseline:
        description: 'Compare against baseline commit (SHA or tag)'
        required: false
        type: string

permissions:
  contents: read

jobs:
  placeholder:
    name: Performance Benchmarks (Disabled)
    runs-on: ubuntu-latest
    steps:
    - name: Notice
      run: |
        echo "Performance benchmarks workflow is temporarily disabled."
        echo "TODO: Refactor inline Python code blocks to external scripts."
        echo "Rust benchmarks are available via: cd asterix-rs && cargo bench"
