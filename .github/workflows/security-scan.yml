name: Security Scan (Python)

# TEMPORARILY DISABLED
# This workflow has YAML syntax issues due to inline Python code blocks.
# The multiline Python strings break YAML parsing (block scalar indentation).
# TODO: Refactor inline Python code to external scripts in scripts/ directory

on:
  workflow_dispatch:
    inputs:
      run_scan:
        description: 'Run security scan (currently disabled)'
        required: false
        default: 'false'

permissions:
  contents: read
  security-events: write

jobs:
  placeholder:
    name: Security Scan (Disabled)
    runs-on: ubuntu-latest
    steps:
    - name: Notice
      run: |
        echo "Security scan workflow is temporarily disabled."
        echo "TODO: Refactor inline Python code blocks to external scripts."
        echo "Note: CodeQL security scanning is still active via codeql.yml"
